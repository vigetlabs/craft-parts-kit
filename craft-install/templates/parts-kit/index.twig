{#-
Root landing for the parts kit directory.

The Navigation service intentionally skips a root-level `index.twig`, so this
file does NOT appear as its own nav node. It exists only as a friendly default
if someone loads the parts directory root directly.
-#}
<p style="font-family: system-ui, sans-serif; color: #52606d;">
    Select a part from the navigation to preview it.
</p>
