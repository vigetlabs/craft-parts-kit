<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Parts Kit — Dev</title>
    <style>
        body { font-family: system-ui, sans-serif; max-width: 40rem; margin: 4rem auto; padding: 0 1.5rem; color: #1f2933; line-height: 1.5; }
        h1 { margin-bottom: 0.25rem; }
        p { color: #52606d; }
        a { color: #0b69a3; }
        ul { padding-left: 1.2rem; }
        code { background: #f0f4f8; padding: 0 4px; border-radius: 3px; }
    </style>
</head>
<body>
    <h1>Parts Kit — development install</h1>
    <p>This is the local Craft CMS install used to develop the Parts Kit plugin.</p>
    <ul>
        <li><a href="/parts-kit">/parts-kit</a> — the component library UI (sample parts included)</li>
        <li><a href="{{ cpUrl('') }}">Control panel</a> — log in with <code>admin</code> / <code>password</code></li>
    </ul>
</body>
</html>
