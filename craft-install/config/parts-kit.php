<?php
/**
 * Parts Kit plugin configuration — DEV ONLY
 *
 * This file lives inside the development Craft install and is excluded from the
 * distributed plugin package. Allowing anonymous access here lets you view
 * `/parts-kit` straight after `ddev setup` without logging in. The plugin's
 * production default is `requireViewPermission => true`.
 *
 * @see \viget\partskit\models\Settings
 */

return [
    'requireViewPermission' => false,
];
